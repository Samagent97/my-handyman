import './custom-button.css'

const Button = (props) => {
    return (
        <button
            className='custom-button'
        >
            <h1>Button</h1>
        </button>
    );
};

export default Button;
